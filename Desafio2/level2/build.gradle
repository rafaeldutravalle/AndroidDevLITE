apply plugin: 'com.android.application'

android {
    compileSdkVersion 30
    defaultConfig {
        applicationId "com.example.seuapp"
        minSdkVersion 21
        targetSdkVersion 30
        versionCode 1
        versionName "1.0"
    }
    // outras configurações
}

dependencies {
    implementation 'com.android.support:appcompat-v7:30.0.0'
    // outras dependências
}

buildscript {
    repositories {
        google() // Adicione isso
        mavenCentral() // Adicione isso
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.4.0' // Verifique se está atualizado
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
